procedure call(x:boolean; y:integer; a:integer) return boolean
{	
    var z : integer;
    var k : boolean;

    z = 3;
    k = x;
    y = 144;

	return true;
}

procedure Main() return boolean
{	
	var a : boolean;

	a = call(true, 3);

	return a;
}

